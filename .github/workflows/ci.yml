name: CI

# WHY THIS EXISTS. The compiler moves daily and this repository does not, so the
# interesting failure is the one where nothing here changed and it broke anyway.
# mere's own CI checks that its main still TYPES the downstream repositories;
# "does it still type" and "do its gates still pass" are different questions,
# and only this can see the second one.

on:
  push:
  pull_request:
  schedule:
    - cron: "0 4 * * 1"

jobs:
  check:
    runs-on: ubuntu-latest
    # Measured at about 90 seconds on a developer machine: four backends over the
    # linear-algebra programs, then the glTF corpus through three more readers.
    # The limit is generous rather than tight because a hosted runner builds the
    # compiler from source first.
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Check out the compiler
        uses: actions/checkout@v4
        with:
          repository: merelang/mere
          path: mere

      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: "5"

      - name: Build mere
        working-directory: mere
        run: opam install . --deps-only --yes && opam exec -- dune build

      # wat2wasm is what turns the Wasm column from SKIPPED into checked, and a
      # skip is a real weakening of the exact comparison -- three backends
      # agreeing is a weaker statement than four.
      - name: Install wabt
        run: sudo apt-get update && sudo apt-get install -y wabt

      # The Khronos validator is the only reader here that can say whether the
      # files themselves are legal glTF; without it that column prints SKIP.
      - name: Install the glTF validator
        run: npm install --no-save gltf-validator

      # PILLOW IS AN INSTRUMENT AND NOT A CONVENIENCE. northstar's "draws" and "lit"
      # columns are counted in Python, gltf_check's second reader is Python, and the
      # texture oracle is Pillow itself. The runner had none, and for the whole life of
      # this workflow northstar reported "50 of 50 read, 0 drew, 0 lit" -- a missing
      # library reading as a black frame. The gate now says so in its own words instead;
      # this is what makes it not have to.
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Pillow
        run: python3 -m pip install --upgrade pillow

      - name: Gates
        env:
          MERE: ${{ github.workspace }}/mere/_build/default/bin/mere.exe
          MERE_SRC: ${{ github.workspace }}/mere
        run: sh scripts/check.sh
